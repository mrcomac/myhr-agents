fastapi==0.115.12
uvicorn==0.34.2
pydantic==2.11.4
strands-agents
strands-agents-tools
boto3>=1.34.0
python-multipart>=0.0.9
pika==1.3.2
minio==7.2.9
redis==5.0.8
requests==2.32.3

# OpenTelemetry — follows SigNoz's Amazon Bedrock guide verbatim:
# https://signoz.io/docs/amazon-bedrock-monitoring/
#
# `opentelemetry-distro` + `opentelemetry-bootstrap -a install` (run in the
# Dockerfile) discover every installed library (fastapi, requests, pika,
# redis, urllib3, logging…) and pip-install the matching instrumentation
# packages automatically — no need to list them here. The four packages
# below are the ones the tutorial calls out explicitly because bootstrap
# doesn't auto-pick them up: httpx, system-metrics, the OTLP exporter, and
# the Bedrock-specific OpenInference instrumentor.
opentelemetry-distro
opentelemetry-exporter-otlp
opentelemetry-instrumentation-httpx
opentelemetry-instrumentation-system-metrics
openinference-instrumentation-bedrock
